# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi
uvicorn[standard]
sqlalchemy
pydantic-settings
python-dotenv
python-jose[cryptography]
passlib[bcrypt]
bcrypt==4.0.1
python-multipart
apscheduler

# ── Scraping & Ingestion ──────────────────────────────────────────────────────
feedparser
requests
beautifulsoup4
lxml
email-validator

# ── AI / NLP Pipeline ─────────────────────────────────────────────────────────
langgraph
langchain-core
langgraph-checkpoint-sqlite
sentence-transformers       # article embeddings
torch                       # required by sentence-transformers
scikit-learn                # DBSCAN clustering
faiss-cpu                   # vector similarity search
numpy
groq                        # LLM API (summarization + sentiment)
transformers                # tokenizers used by sentence-transformers

# ── Translation ───────────────────────────────────────────────────────────────
deep-translator             # Google Translate wrapper — instant, no quota, no key needed
